-r requirements.txt
pytest==9.1.1
pytest-cov==6.0.0
ruff==0.9.2
# so CI exercises the tracing SDK (the [trace] extra); imported lazily by provekit.trace
opentelemetry-sdk>=1.20,<2
# so CI proves auto-instrumentation (one decorator captures a real OpenAI call as a nested span)
openai>=1.40
openinference-instrumentation-openai>=0.1
