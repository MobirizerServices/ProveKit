version: 1
kind: test
name: LangGraph agent smoke test
connection: LangGraph agent
request:
  type: agent
  method: POST
  path: /invoke
  body: { input: "What is 2 + 2? Reply with just the number." }
assertions:
  - type: contains
    value: "4"
  - type: latency_lt
    value: 8000
